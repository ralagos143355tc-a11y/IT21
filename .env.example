# Required: change these before running in production
SESSION_SECRET=replace-with-a-long-random-string-at-least-32-characters
ADMIN_USERNAME=admin
ADMIN_PASSWORD=ChangeMeToAStrongPassword123!

# Set to true when behind HTTPS in production
COOKIE_SECURE=false
